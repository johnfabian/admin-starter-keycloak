interface:
  display_name: "Skills Audit"
  short_description: "Audit typed skills and provider links"
  default_prompt: "Use $skills-audit to categorize shared skills and repair their explicit provider symlinks."
policy:
  allow_implicit_invocation: false
