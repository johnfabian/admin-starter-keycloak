{
  "name": "admin-starter-keycloak",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.1.2",
  "scripts": {
    "build": "dotenv -e .env.development -- corepack pnpm --dir web build",
    "dev": "dotenv -e .env.development -- corepack pnpm --dir web dev",
    "stop-app-containers": "docker compose -f auth-server/docker-compose.yml down && docker compose -f postgres/docker-compose.yml down && docker compose -f local-mail-server/docker-compose.yml down",
    "stop-app": "corepack pnpm stop-app-containers",
    "start-gateway": "docker compose -f local-mail-server/docker-compose.yml up -d --wait && docker compose -f api-gateway/docker-compose.yml --env-file .env.traefik up -d --wait && docker compose -f postgres/docker-compose.gateway.yml --env-file .env.traefik up -d --wait && docker compose -f auth-server/docker-compose.gateway.yml --env-file .env.traefik up -d --build --wait && docker compose -f web/docker-compose.gateway.yml --env-file .env.traefik up -d --build --wait",
    "dev:gateway": "corepack pnpm start-gateway",
    "stop-gateway": "docker compose -f web/docker-compose.gateway.yml down && docker compose -f auth-server/docker-compose.gateway.yml down && docker compose -f postgres/docker-compose.gateway.yml down && docker compose -f api-gateway/docker-compose.yml down && docker compose -f local-mail-server/docker-compose.yml down",
    "dev:gateway:down": "corepack pnpm stop-gateway",
    "gateway:logs": "docker compose -f api-gateway/docker-compose.yml logs -f",
    "auth:gateway:logs": "docker compose -f auth-server/docker-compose.gateway.yml logs -f",
    "db:gateway:logs": "docker compose -f postgres/docker-compose.gateway.yml logs -f",
    "web:build": "dotenv -e .env.development -- corepack pnpm --dir web build",
    "web:start": "corepack pnpm --dir web start",
    "web:typecheck": "dotenv -e .env.development -- corepack pnpm --dir web typecheck",
    "mail:up": "docker compose -f local-mail-server/docker-compose.yml up -d --wait",
    "mail:down": "docker compose -f local-mail-server/docker-compose.yml down",
    "mail:logs": "docker compose -f local-mail-server/docker-compose.yml logs -f",
    "db:up": "docker compose -f postgres/docker-compose.yml --env-file .env.development up -d --wait",
    "db:down": "docker compose -f postgres/docker-compose.yml down",
    "auth:up": "docker compose -f auth-server/docker-compose.yml --env-file .env.development up -d --build --wait",
    "auth:down": "docker compose -f auth-server/docker-compose.yml down",
    "auth:logs": "docker compose -f auth-server/docker-compose.yml logs -f",
    "gateway:up": "docker compose -f api-gateway/docker-compose.yml --env-file .env.traefik up -d --wait",
    "gateway:down": "docker compose -f api-gateway/docker-compose.yml down",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "corepack pnpm format:check && corepack pnpm --dir web lint && dotenv -e .env.development -- corepack pnpm --dir web typecheck",
    "plan:new": "node scripts/create-plan.mjs"
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "prettier": "^3.8.3"
  }
}
