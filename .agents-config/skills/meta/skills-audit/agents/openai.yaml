interface:
  display_name: "Skills Audit"
  short_description: "Audit skills, README catalog, and links"
  default_prompt: "Use $skills-audit to validate the shared skill and README catalogs and repair their provider symlinks."
policy:
  allow_implicit_invocation: false
