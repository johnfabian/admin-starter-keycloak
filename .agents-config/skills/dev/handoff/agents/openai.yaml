interface:
  display_name: "Handoff"
  short_description: "Create and track a resumable checkpoint"
  default_prompt: "Use $handoff to prepare a provider-neutral checkpoint and record whether its GitHub persistence is provisional or verified."
