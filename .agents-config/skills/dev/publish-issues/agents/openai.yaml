interface:
  display_name: "Publish Issues"
  short_description: "Publish approved issues and comments safely"
  default_prompt: "Use $publish-issues to publish an explicitly approved issue-and-comment preview, then verify its durable GitHub records."
policy:
  allow_implicit_invocation: false
