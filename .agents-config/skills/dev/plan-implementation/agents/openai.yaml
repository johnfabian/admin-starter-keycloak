interface:
  display_name: "Plan Implementation"
  short_description: "Create vertical stories and a dependency plan, critique it inde"
  default_prompt: "Use $plan-implementation for the requested feature workflow."
